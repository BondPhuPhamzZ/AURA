@model IEnumerable<AURA.Models.ReimbursementRequest>

<div class="escalation-queue">
    <h4 class="mb-4 form-header">Danh Sách Cần Sếp Duyệt</h4>
    @if (ViewData["LoadError"] is string loadError)
    {
        <div class="alert alert-danger feature-card" style="padding: 16px;">@loadError</div>
    }
    else
    {
        var escalations = Model?.ToList();
        if (escalations == null || escalations.Count == 0)
        {
            <div class="alert alert-info feature-card" style="padding: 16px;">Hiện không có ca nào cần duyệt.</div>
        }
        else
        {
        <div style="overflow-x: auto;">
            <table class="table-aura">
                <thead>
                    <tr>
                        <th>Mã Đơn</th>
                        <th>Nhân Viên</th>
                        <th>Số Tiền</th>
                        <th>Lý do từ AI</th>
                        <th>Chứng Từ</th>
                        <th>Hành Động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in escalations)
                    {
                        var choices = AURA.Services.EscalationWorkflow.ExplainChoices(item.Status);
                        <tr>
                            <td style="font-family: monospace;">@item.Id.Substring(0, 8)...</td>
                            <td style="font-weight: 500;">Đơn Ẩn Danh</td>
                            <td style="font-weight: 500;">@item.ClaimedAmount.ToString("N0") VND</td>
                            <td>
                                <span class="badge bg-warning text-dark">@item.Status</span><br />
                                <span style="color: #b91c1c;">@item.AiReasoning</span><br />
                                <small>@item.ManagerQuestion</small>
                            </td>
                            <td><a href="@item.ImageUrl" target="_blank" rel="noopener">Xem hóa đơn</a></td>
                            <td>
                                <form asp-controller="Reviewer" asp-action="EscalateAction" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@item.Id" />
                                    <input type="hidden" name="decision" value="YES" />
                                    <button type="submit" class="button-ghost-sm success" title="@choices.Yes">Có</button>
                                </form>
                                <form asp-controller="Reviewer" asp-action="EscalateAction" method="post" class="d-inline" style="margin-left: 8px;">
                                    <input type="hidden" name="id" value="@item.Id" />
                                    <input type="hidden" name="decision" value="NO" />
                                    <button type="submit" class="button-ghost-sm danger" title="@choices.No">Không</button>
                                </form>
                                <div><small><strong>Có:</strong> @choices.Yes[4..]</small></div>
                                <div><small><strong>Không:</strong> @choices.No[7..]</small></div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        }
    }
</div>


